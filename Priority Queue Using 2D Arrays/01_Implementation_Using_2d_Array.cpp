// Define a logic to implement priority queue using 2 dimensional arrays.

#include <iostream>
using namespace std;

class PriorityQueue
{
private:
    int capacity;         // Size of Each Array (Queue)
    int highest_priority; // Highest Priority number
    int **ptr;            // 2D array to store each array (Queue)
    int *front;           // Array to store front index of each array (Queue)
    int *rear;            // Array to store rear index of each array (Queue)
};

int main()
{
    return 0;
}